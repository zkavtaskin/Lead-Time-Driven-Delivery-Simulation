
export class ContinuousResult {
    public readonly x : Array<number>;
    public readonly y : number;

    constructor(x : Array<number>, y : number) {
        this.x = x;
        this.y = y;
    }
}